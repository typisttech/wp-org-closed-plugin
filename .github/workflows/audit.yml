name: Audit

on:
  workflow_dispatch:
  schedule:
    - cron: '16 3 * * *'
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  php:
    uses: typisttech/.github/.github/workflows/audit-php.yml@34f0697e0ba455a427ea7103acc927fe3f7ec299 # v6.1.9

  go:
    uses: typisttech/.github/.github/workflows/audit-go.yml@34f0697e0ba455a427ea7103acc927fe3f7ec299 # v6.1.9
